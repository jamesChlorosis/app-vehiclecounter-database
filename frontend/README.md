# Frontend

The runnable dashboard is served from `public/` so AutoPilot can run as a single Node process with no separate frontend build step.

It uses TailwindCSS utility classes and calls the JWT-protected REST API directly.
